// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for MessagingFunction
 */
export interface MessagingFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/resources/messaging/messaging.
 */
export class MessagingFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: MessagingFunctionProps) {
    super(scope, id, {
      description: 'src/resources/messaging/messaging.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/resources/messaging/messaging.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}